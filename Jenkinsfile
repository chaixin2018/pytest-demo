pipeline {
    agent any
    
    stages {
        stage('Build') {
            steps {
                echo 'Hello, Jenkins!'
                sh 'python3 easyDemo.py'
            }
        }
    }
}
